---
phase: 14-limezu-art-integration
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/engine/renderer.ts
  - src/engine/officeLayout.ts
  - src/engine/depthSort.ts
  - src/engine/__tests__/renderer.test.ts
  - src/engine/__tests__/depthSort.test.ts
autonomous: true
requirements:
  - CHAR-03
  - CHAR-04
  - ENV-08
  - ENV-09
  - ENV-10
  - ENV-11
  - ENV-12
  - ENV-13

must_haves:
  truths:
    - "Floor tiles render from LimeZu Room Builder sheets: light tile for offices, dark for War Room, grey for hallways, warm for rec area"
    - "Walls render as LimeZu 3D wall tiles (full 16x16) instead of 2-3px colored strips"
    - "All furniture renders from LimeZu sheets (Generic, Conference Hall, Film Studio, Living Room)"
    - "Personality decorations render from LimeZu sheets matching agent roles"
    - "Characters are foot-center anchored correctly at 32x32 on 16x16 grid (drawX = x - 8)"
    - "Drop shadows are correctly sized for 32x32 character sprites"
    - "Depth sorting works correctly with 32x32 characters (no z-fighting)"
    - "War Room has conference table, office chairs, whiteboard from Conference Hall sheet"
    - "Rec area has water cooler, couch, plants from LimeZu sheets"
  artifacts:
    - path: "src/engine/renderer.ts"
      provides: "Updated render pipeline using multi-sheet atlas for floors, 3D walls, furniture, decorations, characters"
      contains: "getEnvironmentSheetById"
    - path: "src/engine/officeLayout.ts"
      provides: "Updated FurnitureItem types and DECORATIONS keys matching LimeZu atlas"
    - path: "src/engine/depthSort.ts"
      provides: "baseRow calculation compatible with 32x32 characters"
  key_links:
    - from: "src/engine/renderer.ts"
      to: "src/engine/limeZuAtlas.ts"
      via: "imports LIMEZU_ATLAS for floor, wall, furniture, decoration frame lookups"
      pattern: "import.*LIMEZU_ATLAS.*from.*limeZuAtlas"
    - from: "src/engine/renderer.ts"
      to: "src/engine/spriteSheet.ts"
      via: "uses getEnvironmentSheetById() instead of getEnvironmentSheet()"
      pattern: "getEnvironmentSheetById"
    - from: "src/engine/officeLayout.ts"
      to: "src/engine/limeZuAtlas.ts"
      via: "decoration keys match LIMEZU_ATLAS keys"
      pattern: "LIMEZU_ATLAS"
---

<objective>
Migrate the entire renderer from programmatic placeholders and the single environment.png to LimeZu multi-sheet rendering. Floors use Room Builder tiles, walls use 3D wall system, furniture and decorations render from categorized LimeZu theme sheets, and character rendering uses updated 32x32 anchoring.

Purpose: This is the core visual transformation -- after this plan, the office renders entirely with professional pixel art instead of colored rectangles.

Output: Updated `renderer.ts`, `officeLayout.ts`, `depthSort.ts`.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-limezu-art-integration/14-CONTEXT.md
@.planning/phases/14-limezu-art-integration/14-RESEARCH.md
@.planning/phases/14-limezu-art-integration/14-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (limeZuAtlas.ts, limeZuCharFrames.ts, updated spriteSheet.ts) -->

From src/engine/limeZuAtlas.ts (created in Plan 01):
```typescript
export const SHEET_PATHS: Record<string, string>;
export interface SheetFrame { sheetId: string; frame: SpriteFrame; }
export function sf(sheetId: string, col: number, row: number, w?: number, h?: number): SheetFrame;
export const LIMEZU_ATLAS: Record<string, SheetFrame>;
```

From src/engine/spriteSheet.ts (updated in Plan 01):
```typescript
export function getEnvironmentSheetById(sheetId: string): HTMLImageElement | null;
export function getCharacterSheet(characterId: string): HTMLImageElement | null;
export function getCachedSprite(sheet: HTMLImageElement, frame: SpriteFrame, zoom: number): HTMLCanvasElement;
```

From src/engine/types.ts (updated in Plan 01):
```typescript
export const CHAR_SPRITE_W = 32;  // was 24
export const CHAR_SPRITE_H = 32;
export const TILE_SIZE = 16;
```

From src/engine/officeLayout.ts (current):
```typescript
export interface FurnitureItem {
  roomId: string;
  type: 'desk' | 'chair' | 'table' | 'bookshelf' | 'plant' | 'water-cooler' | 'artwork' | 'couch';
  col: number; row: number; width: number; height: number;
  renderHeight?: number;
}
export interface DecorationItem { roomId: string; key: string; col: number; row: number; }
export const FURNITURE: FurnitureItem[];
export const DECORATIONS: DecorationItem[];
export const ROOM_RUGS: RoomRug[];
```

From src/engine/depthSort.ts (current):
```typescript
export interface Renderable { baseRow: number; priority: number; draw(ctx: CanvasRenderingContext2D): void; }
export function buildRenderables(...): Renderable[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update officeLayout.ts furniture types and decoration keys for LimeZu</name>
  <files>src/engine/officeLayout.ts</files>
  <action>
    1. Expand the FurnitureItem `type` union to include new LimeZu-specific types:
       `'desk' | 'chair' | 'table' | 'bookshelf' | 'plant' | 'water-cooler' | 'artwork' | 'couch' | 'monitor' | 'whiteboard' | 'filing-cabinet' | 'conf-table' | 'conf-chair'`

    2. Add an optional `atlasKey` field to `FurnitureItem` for direct LIMEZU_ATLAS lookup:
       `atlasKey?: string;` -- when set, renderer uses this key in LIMEZU_ATLAS instead of type-based switch. This allows specific LimeZu sprites per furniture instance.

    3. Update FURNITURE array entries:
       - Add `atlasKey` to desks: e.g., `atlasKey: 'desk-wood-3wide'` for 3-wide desks, `'desk-wood-2wide'` for 2-wide
       - Add monitors on desks where appropriate (BILLY, Patrik, Sandra, Isaac have monitors on desks)
       - War Room: Change table type to 'conf-table' with atlasKey for Conference Hall table
       - War Room chairs: Change type to 'conf-chair' with atlasKey for Conference Hall chairs
       - War Room: Add whiteboard decoration: `{ roomId: 'war-room', type: 'whiteboard', col: 11, row: 13, width: 2, height: 1, atlasKey: 'conf-whiteboard' }`
       - Wendy's room: Ensure couch + plants match LimeZu living room assets
       - Rec area: Update water cooler, couch, table with atlasKeys

    4. Update DECORATIONS array keys to match LIMEZU_ATLAS keys:
       - Patrik: `'patrik-chart'` -> `'chart-financial'`, `'patrik-monitor'` -> `'monitor-desk'`, `'filing-cabinet'` stays
       - Marcos: `'marcos-lawbooks'` -> `'books-law'`, `'filing-cabinet'` stays
       - Sandra: `'sandra-schedule'` -> `'whiteboard-schedule'`, `'whiteboard'` -> `'whiteboard-sm'`, `'post-it'` -> `'postit-note'`
       - Isaac: `'isaac-scripts'` -> `'scripts-stack'`, `'isaac-corkboard'` -> `'corkboard'`, add Film Studio props (clapboard, camera)
       - Wendy: `'artwork'` -> `'art-motivational'`, `'wendy-cushion'` -> `'cushion-cozy'`
       - BILLY: Add Film Studio props (clapboard, film-reel), update `'artwork'` -> `'art-framed'`
       - Personal touches: Map `'coffee-mug'`, `'pen-holder'`, `'calculator'`, `'photo-frame'`, `'desk-plant'`, `'figurine'`, `'candle'`, `'papers'`, `'water-glass'` to LIMEZU_ATLAS keys from Generic sheet
       - All new decoration keys MUST exist in the LIMEZU_ATLAS defined in Plan 01

    5. Keep ROOM_RUGS as-is (they render as colored overlays, not sprites -- could be replaced with LimeZu rug sprites in a future pass but the user didn't specify this).

    NOTE on floor type: The renderer determines floor tile type by room (office/warroom/hallway/rec). The rec area needs to be identifiable. Currently rec area tiles are in 'hallway' roomId. Add a helper or constant to identify rec area tile coordinates: cols 10-21, rows 23-26. The renderer's `getTileAtlasKey()` will need to check if a hallway tile is in the rec area and use 'floor-rec' instead of 'floor-hallway'.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx vitest run src/engine/__tests__/officeLayout.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - FurnitureItem has atlasKey optional field
    - All furniture entries have appropriate atlasKeys for LimeZu sprites
    - All decoration keys match LIMEZU_ATLAS keys
    - War Room has conference table, chairs, whiteboard
    - Film Studio props in Isaac and BILLY decorations
    - Rec area identifiable by coordinates
  </done>
</task>

<task type="auto">
  <name>Task 2: Overhaul renderer.ts and depthSort.ts for LimeZu multi-sheet rendering</name>
  <files>
    src/engine/renderer.ts,
    src/engine/depthSort.ts,
    src/engine/__tests__/renderer.test.ts,
    src/engine/__tests__/depthSort.test.ts
  </files>
  <action>
    1. Update imports in `renderer.ts`:
       - Add: `import { LIMEZU_ATLAS } from './limeZuAtlas';`
       - Add: `import { getEnvironmentSheetById } from './spriteSheet';`
       - Remove: `getEnvironmentSheet` import (replaced by getEnvironmentSheetById)
       - Keep: `CHARACTER_FRAMES` import from spriteAtlas (already remapped in Plan 01)
       - Keep: `PLACEHOLDER_COLORS` import for fallback

    2. Update Layer 2 (Floor Tiles):
       - Replace `const envSheet = getEnvironmentSheet();` with multi-sheet lookups
       - Update `getTileAtlasKey()` to distinguish rec area floors:
         ```
         case TileType.FLOOR: {
           const room = getRoomAtTile(col, row);
           if (room?.id === 'war-room') return 'floor-warroom';
           if (!room) {
             // Check if in rec area (cols 10-21, rows 23-26)
             if (col >= 10 && col <= 21 && row >= 23 && row <= 26) return 'floor-rec';
             return 'floor-hallway';
           }
           return 'floor-office';
         }
         ```
       - For each floor tile, look up `LIMEZU_ATLAS[atlasKey]` to get `SheetFrame`, then use `getEnvironmentSheetById(sf.sheetId)` to get the correct sheet, and draw from `sf.frame`.
       - Create a helper function `drawLimeZuTile(ctx, atlasKey, x, y, w?, h?)` that does the LIMEZU_ATLAS lookup + sheet fetch + drawImage. This avoids repeating the pattern everywhere.
       - Door tiles: Use a specific LimeZu door tile from the atlas, or render as floor tile (doors are visually just floor tiles with a different marking -- per current behavior they use a 'door' atlas key).

    3. Update Layer 3 (3D Wall System):
       - COMPLETELY REWRITE `renderWalls()`. Instead of drawing 2-3px colored strips, draw full 16x16 LimeZu 3D wall tiles.
       - The 3D wall system needs tiles for:
         - North-facing walls (south neighbor is floor): Draw a 3D wall tile that shows the wall face from above. Use the full tile at the wall position. Also draw a shadow tile on the floor below (from floor-shadows sheet).
         - East-facing walls (west neighbor is floor): Draw a side-facing 3D wall tile.
         - West-facing walls (east neighbor is floor): Draw a side-facing 3D wall tile (mirrored or different variant).
         - Corner tiles: Where north + east or north + west walls meet.
       - Wall color: Use a single wall color variant from the 3D walls sheet that works with light tile floors and dark UI theme. Per user's discretion note, pick a neutral/warm tone.
       - Remove `WALL_COLOR_OFFICE`, `WALL_COLOR_WAR_ROOM`, `WALL_SHADOW` constants (replaced by tile-based rendering).
       - Keep `getWallColor()` as fallback or remove it.

    4. Update Layer 4 renderables:
       - `renderFurnitureItemWorld()`: Replace the switch-based rendering with atlas-key lookup.
         If `item.atlasKey` exists, look up `LIMEZU_ATLAS[item.atlasKey]`, get sheet via `getEnvironmentSheetById()`, draw the sprite. For multi-tile furniture, the atlas entry should have the full sprite dimensions (e.g., a 3-wide desk is w=3 tiles in the SheetFrame).
         Keep the switch as fallback for items without atlasKey.
       - `renderDecorationWorld()`: Update to use `LIMEZU_ATLAS[dec.key]` instead of `DECORATION_ATLAS[dec.key] ?? ENVIRONMENT_ATLAS[dec.key]`. Get sheet via `getEnvironmentSheetById()`.

    5. Update character rendering:
       - `renderCharacterWorld()`: The foot-center anchor formula `drawX = x - (CHAR_SPRITE_W - TILE_SIZE) / 2` now produces `x - 8` (was `x - 4`) automatically since CHAR_SPRITE_W changed to 32 in Plan 01. No formula change needed, just verify the comment.
       - Update drop shadow: Change `shadowRx = TILE_SIZE * 0.4` to `TILE_SIZE * 0.5` for slightly wider shadow matching 32x32 sprite width.
       - Update fallback rectangle dimensions (already uses CHAR_SPRITE_W/H constants, so should auto-adjust).
       - Verify the CHARACTER_FRAMES lookup still works (it does -- Plan 01 made it a re-export of LIMEZU_CHARACTER_FRAMES with same type signature).

    6. Update `depthSort.ts`:
       - Review character baseRow calculation: `const footRow = ch.y / TILE_SIZE;` then `baseRow: footRow + 1`. This uses ch.y (top of occupied tile) not sprite dimensions, so it should be correct for 32x32. The sprite is taller but the foot position (ch.y + TILE_SIZE) hasn't changed. Verify and add a comment confirming 32x32 compatibility.

    7. Remove deprecated references:
       - Remove `getEnvironmentSheet()` calls (replaced by getEnvironmentSheetById)
       - Remove or deprecate the old `renderFurnitureSprite()` switch function if fully replaced by atlas-key approach
       - Clean up old `ENVIRONMENT_ATLAS` and `DECORATION_ATLAS` references in renderer.ts

    8. Update `src/engine/__tests__/renderer.test.ts` (CHAR-03 coverage):
       - Add or update tests verifying the 32x32 character anchor math: `drawX = x - (CHAR_SPRITE_W - TILE_SIZE) / 2` equals `x - 8` with CHAR_SPRITE_W=32
       - Test that drop shadow radius uses TILE_SIZE * 0.5 (updated for 32x32)
       - Test that `drawLimeZuTile` helper correctly looks up LIMEZU_ATLAS keys and calls getEnvironmentSheetById
       - Existing renderer tests should still pass

    9. Create `src/engine/__tests__/depthSort.test.ts` (CHAR-04 coverage):
       - Test that character baseRow calculation is correct for 32x32 sprites: footRow = ch.y / TILE_SIZE, baseRow = footRow + 1
       - Test that furniture baseRow uses row + height (unchanged)
       - Test that characters and furniture at the same row sort correctly by priority
       - Test that a character at a lower row renders before furniture at a higher row (depth ordering)

    IMPORTANT: Keep PLACEHOLDER_COLORS fallback in renderCharacterWorld and floor rendering for graceful degradation if sheets haven't loaded. The pattern should be: try LimeZu atlas -> try old atlas -> fallback color.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx vitest run src/engine/__tests__/renderer.test.ts src/engine/__tests__/depthSort.test.ts --reporter=verbose && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Floor tiles render from LimeZu Room Builder (4 distinct floor types visible)
    - Walls render as 3D LimeZu tiles (no more colored strips)
    - Furniture renders from LimeZu theme sheets (Generic, Conference Hall, Film Studio)
    - Decorations render from LimeZu sheets with personality-specific items
    - Characters correctly anchored at 32x32 with wider shadow
    - depthSort compatible with 32x32 characters
    - renderer.test.ts validates 32x32 anchor math and multi-sheet rendering (CHAR-03)
    - depthSort.test.ts validates baseRow calculation for 32x32 characters (CHAR-04)
    - Full test suite passes, TypeScript compiles
    - No programmatic placeholder rendering visible (except as fallback if sheets fail to load)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx tsc --noEmit` -- TypeScript compiles cleanly
- `npm run dev` -- app starts, canvas renders with LimeZu tiles
- Visual check: floors show different tile patterns per room type, walls have 3D depth, furniture is pixel art
</verification>

<success_criteria>
- Zero programmatic placeholder sprites visible on canvas (except graceful fallback)
- 4 distinct floor types: light (offices), dark (War Room), grey (hallways), warm (rec area)
- 3D wall tiles replace all 2-3px colored strips
- Conference Hall assets in War Room (table, chairs, whiteboard)
- Film Studio props in Isaac and/or BILLY offices
- Characters correctly sized and anchored at 32x32
- Drop shadows appropriately sized
- renderer.test.ts covers CHAR-03 (32x32 anchor math)
- depthSort.test.ts covers CHAR-04 (32x32 depth sorting)
- All existing tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-limezu-art-integration/14-02-SUMMARY.md`
</output>
