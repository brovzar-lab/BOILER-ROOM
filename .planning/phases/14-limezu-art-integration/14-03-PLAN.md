---
phase: 14-limezu-art-integration
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - src/engine/renderer.ts
  - src/engine/spriteAtlas.ts
autonomous: false
requirements:
  - UI-06
  - UI-07

must_haves:
  truths:
    - "Agent thinking status shows LimeZu thinking emote sprite above character head instead of amber dots"
    - "Agent needs-attention status shows LimeZu speech bubble sprite instead of programmatic white bubble"
    - "Emotes and speech bubbles render correctly at all zoom levels"
    - "No programmatic placeholder rendering remains anywhere on the canvas"
  artifacts:
    - path: "src/engine/renderer.ts"
      provides: "Status overlay rendering using LimeZu UI sprites"
      contains: "getEnvironmentSheetById"
    - path: "src/engine/spriteAtlas.ts"
      provides: "Cleaned up -- deprecated ENVIRONMENT_ATLAS and DECORATION_ATLAS removed"
  key_links:
    - from: "src/engine/renderer.ts"
      to: "src/engine/limeZuAtlas.ts"
      via: "LIMEZU_ATLAS keys emote-thinking, speech-bubble for status overlays"
      pattern: "LIMEZU_ATLAS\\['emote-"
---

<objective>
Replace programmatic status overlays (amber thinking dots, white speech bubble) with LimeZu UI emote and speech bubble sprites. Clean up deprecated atlas code. Visual verification checkpoint to confirm the full LimeZu art integration looks correct.

Purpose: Final visual polish and cleanup. After this plan, every pixel on the canvas comes from LimeZu assets.

Output: Updated `renderer.ts` with LimeZu UI overlays, cleaned `spriteAtlas.ts`, visual verification sign-off.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-limezu-art-integration/14-CONTEXT.md
@.planning/phases/14-limezu-art-integration/14-RESEARCH.md
@.planning/phases/14-limezu-art-integration/14-01-SUMMARY.md
@.planning/phases/14-limezu-art-integration/14-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 limeZuAtlas.ts -->
From src/engine/limeZuAtlas.ts:
```typescript
export const LIMEZU_ATLAS: Record<string, SheetFrame>;
// UI keys expected: emote-thinking, emote-exclamation, speech-bubble-left, speech-bubble-right
```

From src/engine/spriteSheet.ts:
```typescript
export function getEnvironmentSheetById(sheetId: string): HTMLImageElement | null;
export function getCachedSprite(sheet: HTMLImageElement, frame: SpriteFrame, zoom: number): HTMLCanvasElement;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace status overlays with LimeZu UI sprites and clean up deprecated code</name>
  <files>src/engine/renderer.ts, src/engine/spriteAtlas.ts</files>
  <action>
    1. Update `renderStatusOverlays()` in renderer.ts:

       **Thinking status (currently amber dots):**
       - Look up `LIMEZU_ATLAS['emote-thinking']` to get SheetFrame
       - Get the sheet via `getEnvironmentSheetById(sf.sheetId)` (should be 'ui-emotes')
       - Use `getCachedSprite()` for zoom-appropriate rendering
       - Position the emote sprite above the character's head at screen coordinates
       - The emote is a 16x16 sprite. Scale it by zoom for screen rendering: `size = 16 * zoom * 0.8` (slightly smaller than a full tile to not overwhelm)
       - Draw at: `cx - size/2, charScreenY - size - 2*zoom` (centered above head)
       - Remove the old amber dots rendering code

       **Needs-attention status (currently white bubble + red dot):**
       - Look up `LIMEZU_ATLAS['speech-bubble-left']` (or right, depending on visual preference)
       - Get the sheet and use getCachedSprite for zoom rendering
       - Position above character head, slightly offset
       - The speech bubble sprite likely includes the pointer/tail
       - Add a red notification dot on top of the speech bubble (keep this -- it's a functional indicator, not just art)
       - Remove the old programmatic bubble rendering code

       - Keep the `worldToScreen()` positioning approach -- sprites render at screen coords like before
       - Static emotes only per user decision (no animated emote cycling -- deferred to v2.1)

    2. Clean up `src/engine/spriteAtlas.ts`:
       - Remove `ENVIRONMENT_ATLAS` (all references migrated to LIMEZU_ATLAS in Plan 02)
       - Remove `DECORATION_ATLAS` (all references migrated in Plan 02)
       - Remove `makeEnvFrame()` helper (no longer needed)
       - Keep `CHARACTER_FRAMES` re-export and `CHARACTER_SHEET_NAMES`
       - Update module-level JSDoc comment to reflect LimeZu architecture

    3. Verify no remaining references to old atlases:
       - Search renderer.ts for any `ENVIRONMENT_ATLAS` or `DECORATION_ATLAS` references -- should be zero
       - Search for any `getEnvironmentSheet()` (without ById) -- should be zero
       - Ensure all imports are clean
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Thinking status shows LimeZu emote sprite (not amber dots)
    - Needs-attention shows LimeZu speech bubble (not programmatic white bubble)
    - ENVIRONMENT_ATLAS and DECORATION_ATLAS removed from spriteAtlas.ts
    - No references to old single-sheet getEnvironmentSheet() remain
    - All tests pass, TypeScript compiles
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete LimeZu art integration</name>
  <files>N/A</files>
  <action>
    Present the completed LimeZu art integration for human visual verification.
    All programmatic sprites have been replaced with professional LimeZu Modern Interiors pixel art.
    Run `npm run dev` and guide the user through the verification checklist.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>User confirms visual quality is acceptable and all LimeZu art renders correctly</done>
  <what-built>Complete LimeZu art integration replacing all programmatic sprites with professional pixel art. Characters are 32x32 LimeZu premade sprites. Floors, walls, furniture, and decorations all render from LimeZu Modern Interiors sheets. Status indicators use LimeZu UI emotes and speech bubbles.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. Verify characters:
       - All 6 characters (BILLY, Patrik, Marcos, Sandra, Isaac, Wendy) appear as distinct pixel art sprites
       - Characters walk smoothly with walk animations (click around to move BILLY)
       - Characters are properly anchored (feet on floor, no floating or sinking)
       - Drop shadows visible under each character
    3. Verify floors:
       - Agent offices have light tile/marble floors
       - War Room has a darker/premium floor
       - Hallways/corridors have dark grey floors
       - Rec area (below War Room) has warm wood/carpet floor
    4. Verify walls:
       - Walls show 3D perspective depth (not flat colored strips)
       - Wall color is consistent and works with the dark UI theme
       - No visual gaps between walls and floors
    5. Verify furniture:
       - Desks are modern wood style from LimeZu
       - War Room has conference table, office chairs, and whiteboard
       - Wendy's room has couch + plants layout
       - Rec area has water cooler, seating, plants
    6. Verify decorations:
       - Patrik: financial charts, filing cabinet visible
       - Marcos: bookshelves, law books
       - Sandra: whiteboard, production items
       - Isaac: corkboard + Film Studio props (camera, clapboard)
       - BILLY: CEO decor + Film Studio props
       - Wendy: motivational art, plants, cozy items
    7. Verify UI overlays:
       - Send a message to an agent and verify thinking indicator shows LimeZu emote sprite
       - Check that speech bubble overlay uses LimeZu sprite
    8. Zoom in/out and verify everything renders correctly at multiple zoom levels
    9. Verify no colored rectangles or programmatic placeholders visible anywhere
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe specific visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx tsc --noEmit` -- clean compilation
- Visual inspection confirms all 13 requirements met
</verification>

<success_criteria>
- LimeZu emote sprites replace amber thinking dots
- LimeZu speech bubble sprites replace programmatic white bubbles
- Zero programmatic placeholder sprites visible on the canvas
- No deprecated atlas code remains in spriteAtlas.ts
- Human visual verification confirms professional art quality
- All 13 phase requirements (CHAR-01 through CHAR-04, ENV-08 through ENV-14, UI-06, UI-07) verified
</success_criteria>

<output>
After completion, create `.planning/phases/14-limezu-art-integration/14-03-SUMMARY.md`
</output>
