---
phase: 14-limezu-art-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/limeZuAtlas.ts
  - src/engine/limeZuCharFrames.ts
  - src/engine/types.ts
  - src/engine/spriteSheet.ts
  - src/engine/spriteAtlas.ts
  - src/engine/__tests__/limeZuAtlas.test.ts
  - src/engine/__tests__/spriteSheet.test.ts
autonomous: true
requirements:
  - CHAR-01
  - CHAR-02
  - ENV-14

must_haves:
  truths:
    - "CHAR_SPRITE_W and CHAR_SPRITE_H are both 32 (was 24x32)"
    - "Multi-sheet loader loads all LimeZu PNG sheets in parallel and stores them by sheetId"
    - "CHARACTER_FRAMES maps idle/walk/sit/work states x 4 directions to correct LimeZu premade sheet coordinates for all 6 characters"
    - "LIMEZU_ATLAS maps semantic keys (floor-office, desk-wood, etc.) to {sheetId, frame} tuples"
    - "SHEET_PATHS maps sheetId strings to correct /sprites/modern-interiors-paid/ paths"
  artifacts:
    - path: "src/engine/limeZuAtlas.ts"
      provides: "Multi-sheet atlas registry with SHEET_PATHS, LIMEZU_ATLAS, SheetFrame type"
      exports: ["SHEET_PATHS", "LIMEZU_ATLAS", "SheetFrame", "sf"]
    - path: "src/engine/limeZuCharFrames.ts"
      provides: "LimeZu character frame mapping for premade 32x32 sheets"
      exports: ["LIMEZU_CHARACTER_FRAMES", "CHAR_SHEET_PATHS"]
    - path: "src/engine/types.ts"
      provides: "Updated dimension constants"
      contains: "CHAR_SPRITE_W = 32"
    - path: "src/engine/spriteSheet.ts"
      provides: "Multi-sheet environment loader with getEnvironmentSheetById()"
      exports: ["getEnvironmentSheetById"]
    - path: "src/engine/spriteAtlas.ts"
      provides: "CHARACTER_FRAMES re-exported from limeZuCharFrames"
  key_links:
    - from: "src/engine/limeZuAtlas.ts"
      to: "src/engine/spriteSheet.ts"
      via: "SHEET_PATHS imported for parallel loading"
      pattern: "SHEET_PATHS"
    - from: "src/engine/limeZuCharFrames.ts"
      to: "src/engine/spriteAtlas.ts"
      via: "CHARACTER_FRAMES re-export"
      pattern: "LIMEZU_CHARACTER_FRAMES"
    - from: "src/engine/spriteSheet.ts"
      to: "src/engine/limeZuAtlas.ts"
      via: "imports SHEET_PATHS for environment sheet loading"
      pattern: "import.*SHEET_PATHS.*from.*limeZuAtlas"
---

<objective>
Create the LimeZu asset foundation: multi-sheet atlas registry, character frame mapping for 32x32 premade sheets, multi-sheet loader, and dimension constant updates.

Purpose: All subsequent rendering tasks depend on these data structures and the multi-sheet loading infrastructure. This plan creates the contracts (types, coordinate maps, loader API) that Plan 02 and 03 consume.

Output: `limeZuAtlas.ts`, `limeZuCharFrames.ts` (new files), updated `types.ts`, `spriteSheet.ts`, `spriteAtlas.ts`.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-limezu-art-integration/14-CONTEXT.md
@.planning/phases/14-limezu-art-integration/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/engine/types.ts:
```typescript
export const TILE_SIZE = 16;
export const CHAR_SPRITE_W = 24;  // CHANGE TO 32
export const CHAR_SPRITE_H = 32;  // stays 32
export type Direction = 'up' | 'down' | 'left' | 'right';
export type CharacterState = 'idle' | 'walk' | 'work';
export interface SpriteFrame { x: number; y: number; w: number; h: number; }
```

From src/engine/spriteAtlas.ts:
```typescript
export const CHARACTER_FRAMES: Record<CharacterState | 'talk', Record<Direction, SpriteFrame[]>>;
export const CHARACTER_SHEET_NAMES: string[];
export const ENVIRONMENT_ATLAS: Record<string, SpriteFrame>;
export const DECORATION_ATLAS: Record<string, SpriteFrame>;
```

From src/engine/spriteSheet.ts:
```typescript
export function loadSpriteSheet(src: string): Promise<HTMLImageElement>;
export async function loadAllAssets(): Promise<void>;
export function getCharacterSheet(characterId: string): HTMLImageElement | null;
export function getEnvironmentSheet(): HTMLImageElement | null;
export function getCachedSprite(sheet: HTMLImageElement, frame: SpriteFrame, zoom: number): HTMLCanvasElement;
export function clearSpriteCache(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LimeZu atlas registry and character frame mapping</name>
  <files>
    src/engine/limeZuAtlas.ts,
    src/engine/limeZuCharFrames.ts,
    src/engine/types.ts,
    src/engine/__tests__/limeZuAtlas.test.ts
  </files>
  <behavior>
    - CHAR_SPRITE_W equals 32 (updated from 24)
    - SHEET_PATHS contains keys: generic, living-room, classroom, conference, film-studio, floors, walls, 3d-walls, baseboards, floor-shadows, ui, ui-emotes
    - Each SHEET_PATHS value is a string starting with '/sprites/modern-interiors-paid/'
    - LIMEZU_ATLAS contains floor keys: floor-office, floor-warroom, floor-hallway, floor-rec
    - LIMEZU_ATLAS contains wall keys for 3D wall tiles
    - LIMEZU_ATLAS contains furniture keys: desk-wood-2wide, desk-wood-3wide, chair-office, bookshelf-2tall, table-conference, monitor, couch-2wide, plant-potted, water-cooler, filing-cabinet
    - LIMEZU_ATLAS contains War Room keys: conf-table, conf-chair, whiteboard
    - LIMEZU_ATLAS contains decoration keys matching personality items from CONTEXT.md
    - LIMEZU_ATLAS contains UI keys: emote-thinking, emote-exclamation, speech-bubble-left, speech-bubble-right
    - LIMEZU_ATLAS contains film studio keys: clapboard, camera, film-reel, director-chair
    - All LIMEZU_ATLAS entries have valid sheetId referencing a key in SHEET_PATHS
    - LIMEZU_CHARACTER_FRAMES maps idle/walk/work/talk x 4 directions to SpriteFrame arrays
    - CHAR_SHEET_PATHS maps character IDs (billy, patrik, marcos, sandra, isaac, wendy) to premade character PNG paths
  </behavior>
  <action>
    1. Update `src/engine/types.ts`: Change `CHAR_SPRITE_W = 24` to `CHAR_SPRITE_W = 32`. Leave CHAR_SPRITE_H at 32.

    2. Create `src/engine/limeZuAtlas.ts`:
       - Define `SHEET_PATHS: Record<string, string>` mapping sheetId to path under `/sprites/modern-interiors-paid/`. Paths confirmed in research:
         - 'generic': `1_Interiors/16x16/Theme_Sorter/1_Generic_16x16.png`
         - 'living-room': `1_Interiors/16x16/Theme_Sorter/2_LivingRoom_16x16.png`
         - 'classroom': `1_Interiors/16x16/Theme_Sorter/5_Classroom_and_library_16x16.png`
         - 'conference': `1_Interiors/16x16/Theme_Sorter/13_Conference_Hall_16x16.png`
         - 'film-studio': `1_Interiors/16x16/Theme_Sorter/23_Television_and_Film_Studio.png`
         - 'floors': `1_Interiors/16x16/Room_Builder_subfiles/Room_Builder_Floors_16x16.png`
         - 'walls': `1_Interiors/16x16/Room_Builder_subfiles/Room_Builder_Walls_16x16.png`
         - '3d-walls': `1_Interiors/16x16/Room_Builder_subfiles/Room_Builder_3d_walls_16x16.png`
         - 'baseboards': `1_Interiors/16x16/Room_Builder_subfiles/Room_Builder_Baseboards_16x16.png`
         - 'floor-shadows': `1_Interiors/16x16/Room_Builder_subfiles/Room_Builder_Floor_Shadows_16x16.png`
         - 'ui': `4_User_Interface_Elements/UI_16x16.png`
         - 'ui-emotes': `4_User_Interface_Elements/UI_thinking_emotes_animation_16x16.png`

       - Define `SheetFrame` interface: `{ sheetId: string; frame: SpriteFrame }`
       - Create helper `sf(sheetId, col, row, w?, h?)` returning SheetFrame
       - Define `LIMEZU_ATLAS: Record<string, SheetFrame>` with all environment, furniture, decoration, and UI entries.
       - CRITICAL: Exact tile coordinates must be determined by visually inspecting each LimeZu PNG sheet. Load each sheet image using the Read tool to identify correct column/row positions. The research provides sheet dimensions (e.g., floors is 240x640 = 15 cols x 40 rows) but exact tile positions need visual confirmation.
       - Floor tiles: Pick center/repeatable tiles from each floor style group in Room_Builder_Floors_16x16.png (15 cols x 40 rows). Light tile/marble for offices, dark premium for War Room, grey/charcoal for hallways, warm wood for rec area.
       - 3D wall tiles: Pick one wall color band from Room_Builder_3d_walls_16x16.png (24 cols x 59 rows) that works with light tile floors and dark UI theme. Need north-facing and side-facing variants.
       - Furniture: Map from 1_Generic_16x16.png (16 cols x 78 rows). Wood desks, office chairs, monitors, bookshelves, plants, filing cabinets, water coolers.
       - Conference Hall: Map from 13_Conference_Hall_16x16.png (16 cols x 12 rows). Conference table, office chairs, whiteboard.
       - Film Studio: Map from 23_Television_and_Film_Studio.png (16 cols x 14 rows). Clapboards, cameras, film reels, director chairs.
       - Rec area: Couches from 2_LivingRoom_16x16.png (16 cols x 45 rows).
       - Decorations: Map personality items from Generic + Classroom/Library + Film Studio sheets.
       - UI: Map emote sprites from UI_thinking_emotes_animation_16x16.png (10 cols x 10 rows). Map speech bubble sprites from UI_16x16.png (18 cols x 16 rows).

    3. Create `src/engine/limeZuCharFrames.ts`:
       - Define `CHAR_SHEET_PATHS: Record<string, string>` mapping character IDs to premade character PNG paths:
         - billy: `Premade_Character_32x32_13.png`
         - patrik: `Premade_Character_32x32_05.png`
         - marcos: `Premade_Character_32x32_09.png`
         - sandra: `Premade_Character_32x32_08.png`
         - isaac: `Premade_Character_32x32_03.png`
         - wendy: `Premade_Character_32x32_10.png`
       - Define `LIMEZU_CHARACTER_FRAMES: Record<CharacterState | 'talk', Record<Direction, SpriteFrame[]>>`.
       - CRITICAL: The LimeZu 32x32 premade sheets are 1792x1312 = 56 cols x 41 rows. Load a character sheet image using the Read tool and visually audit exact frame boundaries per animation row. The research indicates:
         - Row 0: idle preview (4 frames, one per direction: down, left, right, up)
         - Row 1: full idle loop (directions packed sequentially within the row)
         - Row 2: full walk loop (directions packed sequentially within the row)
         - Rows 4-5: sit animations
         - Row 6: phone animation (use for 'work' state)
       - For each animation, determine: how many frames per direction, which columns each direction starts at. Use the idle preview row (row 0) as the simplest reference -- 4 frames at cols 0-3 = down, left, right, up.
       - For walk animation (row 2), directions are likely packed as groups of frames: down(N frames), left(N frames), right(N frames), up(N frames). Count exact frame count per direction.
       - For now, use 2-4 frames per direction for walk (enough for smooth animation) and 1 frame for idle (from row 0 preview).

    4. Create test file `src/engine/__tests__/limeZuAtlas.test.ts`:
       - Test all SHEET_PATHS values start with '/sprites/modern-interiors-paid/'
       - Test all LIMEZU_ATLAS entries reference valid sheetId present in SHEET_PATHS
       - Test all frame coordinates are non-negative and within expected sheet pixel dimensions
       - Test required keys exist in LIMEZU_ATLAS (floor-office, desk-wood-2wide, conf-table, emote-thinking, etc.)
       - Test LIMEZU_CHARACTER_FRAMES has entries for all 4 states x 4 directions
       - Test CHAR_SHEET_PATHS has all 6 character IDs
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx vitest run src/engine/__tests__/limeZuAtlas.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - types.ts has CHAR_SPRITE_W = 32
    - limeZuAtlas.ts exports SHEET_PATHS (12 sheets), LIMEZU_ATLAS (40+ keys), SheetFrame type
    - limeZuCharFrames.ts exports LIMEZU_CHARACTER_FRAMES (4 states x 4 dirs) and CHAR_SHEET_PATHS (6 characters)
    - All atlas tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update spriteSheet.ts multi-sheet loader and spriteAtlas.ts re-exports</name>
  <files>
    src/engine/spriteSheet.ts,
    src/engine/spriteAtlas.ts,
    src/engine/__tests__/spriteSheet.test.ts
  </files>
  <action>
    1. Update `src/engine/spriteSheet.ts`:
       - Import `SHEET_PATHS` from `./limeZuAtlas` and `CHAR_SHEET_PATHS` from `./limeZuCharFrames`
       - Replace single `environmentSheet` variable with `const environmentSheets: Map<string, HTMLImageElement> = new Map()`
       - Update `loadAllAssets()`:
         - Load character sheets from `CHAR_SHEET_PATHS` values (full paths under `/sprites/modern-interiors-paid/2_Characters/...`)
         - Load environment sheets from `SHEET_PATHS` values in parallel using existing `loadSpriteSheet()`
         - Store environment sheets in `environmentSheets` Map keyed by sheetId
       - Add `getEnvironmentSheetById(sheetId: string): HTMLImageElement | null` that returns from the Map
       - Keep `getEnvironmentSheet()` as a deprecated compat wrapper (returns null since single sheet no longer exists) -- or remove it and let the compiler find all callsites. The renderer will be updated in Plan 02 to use `getEnvironmentSheetById()`.
       - Keep `getCharacterSheet()` working -- it already uses characterSheets Map, just update the loading paths.
       - Keep PLACEHOLDER_COLORS for fallback rendering.

    2. Update `src/engine/spriteAtlas.ts`:
       - Import `LIMEZU_CHARACTER_FRAMES` from `./limeZuCharFrames`
       - Replace the old `buildCharacterFrames()` function and `CHARACTER_FRAMES` const with a re-export: `export const CHARACTER_FRAMES = LIMEZU_CHARACTER_FRAMES;`
       - Update `CHARACTER_SHEET_NAMES` to use the keys from `CHAR_SHEET_PATHS`
       - Keep `ENVIRONMENT_ATLAS` and `DECORATION_ATLAS` temporarily (renderer still references them until Plan 02 migrates to LIMEZU_ATLAS). Mark them with `@deprecated` JSDoc comments.
       - Remove or update the SPRT-04 asset swap contract comment at the top to reflect the new LimeZu layout.

    3. Update `src/engine/__tests__/spriteSheet.test.ts`:
       - Add test that `getEnvironmentSheetById` returns null for unknown sheetId
       - Verify CHARACTER_SHEET_NAMES contains all 6 character IDs
       - Existing tests should still pass (loadAllAssets, getCharacterSheet, getCachedSprite patterns)
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx vitest run src/engine/__tests__/spriteSheet.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - spriteSheet.ts loads 6 character sheets from LimeZu premade paths and 12 environment sheets from SHEET_PATHS
    - getEnvironmentSheetById() exported and callable
    - spriteAtlas.ts CHARACTER_FRAMES uses LimeZu frame mapping
    - Old environment atlas remains as deprecated compat (removed in Plan 02)
    - All spriteSheet tests pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/engine/__tests__/limeZuAtlas.test.ts --reporter=verbose` -- all atlas structure tests green
- `npx vitest run src/engine/__tests__/spriteSheet.test.ts --reporter=verbose` -- all loader tests green
- `npx vitest run --reporter=verbose` -- full suite passes (no regressions from CHAR_SPRITE_W change)
- TypeScript compiles: `npx tsc --noEmit`
</verification>

<success_criteria>
- CHAR_SPRITE_W = 32 in types.ts
- limeZuAtlas.ts has 12 sheet paths and 40+ atlas entries with valid coordinates
- limeZuCharFrames.ts maps all 6 characters with idle/walk/sit/work/talk frames
- spriteSheet.ts loads multiple environment sheets and exposes getEnvironmentSheetById()
- spriteAtlas.ts CHARACTER_FRAMES uses LimeZu mapping
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/14-limezu-art-integration/14-01-SUMMARY.md`
</output>
