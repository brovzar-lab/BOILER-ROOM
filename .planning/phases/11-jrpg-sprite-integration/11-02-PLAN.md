---
phase: 11-jrpg-sprite-integration
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - scripts/generateSprites.ts
  - src/engine/spriteAtlas.ts
  - src/engine/types.ts
  - public/sprites/billy.png
  - public/sprites/patrik.png
  - public/sprites/marcos.png
  - public/sprites/sandra.png
  - public/sprites/isaac.png
  - public/sprites/wendy.png
  - public/sprites/environment.png
autonomous: true
requirements:
  - SPRT-01
  - SPRT-02
  - SPRT-03
  - SPRT-04
  - ENV-01
  - ENV-02

must_haves:
  truths:
    - "Character sprite PNGs are 24x32 pixels per frame with visible face detail and outfit texture"
    - "Each character has idle, walk (4 directions), work, and talk animation frames"
    - "Environment tile sheet contains warm parquet floor and 3/4 furniture with visible front faces"
    - "Sprite atlas frame coordinates match the generated PNG layouts exactly"
    - "New PNGs can be dropped into public/sprites/ to replace programmatic art without code changes"
  artifacts:
    - path: "scripts/generateSprites.ts"
      provides: "24x32 character sprite generator + 3/4 environment tile generator"
      min_lines: 400
    - path: "src/engine/spriteAtlas.ts"
      provides: "Updated frame coordinates for 24x32 characters + expanded environment atlas"
      exports: ["CHARACTER_FRAMES", "CHARACTER_SHEET_NAMES", "ENVIRONMENT_ATLAS", "DECORATION_ATLAS"]
    - path: "src/engine/types.ts"
      provides: "CHAR_SPRITE_W=24 and CHAR_SPRITE_H=32 constants"
      contains: "CHAR_SPRITE_W.*24"
    - path: "public/sprites/billy.png"
      provides: "Billy 24x32 sprite sheet"
    - path: "public/sprites/environment.png"
      provides: "3/4 perspective environment tile sheet"
  key_links:
    - from: "src/engine/spriteAtlas.ts"
      to: "scripts/generateSprites.ts"
      via: "Frame coordinates must match generated sprite sheet layout"
      pattern: "CHAR_SPRITE_W|CHAR_SPRITE_H|24|32"
    - from: "src/engine/spriteAtlas.ts"
      to: "src/engine/types.ts"
      via: "Imports CHAR_SPRITE_W/H for frame dimensions"
      pattern: "CHAR_SPRITE_W|CHAR_SPRITE_H"
---

<objective>
Rewrite the sprite generation pipeline for 24x32 JRPG characters and 3/4 perspective environment tiles, update the sprite atlas for new dimensions, and generate all sprite PNGs.

Purpose: Replace 16x16 flat sprites with 24x32 JRPG 3/4 perspective art (programmatic placeholders at correct dimensions — real art swapped later via SPRT-04 asset pipeline).
Output: 6 character sprite sheet PNGs (24x32 per frame), 1 environment tile sheet with 3/4 furniture, updated spriteAtlas.ts and types.ts.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-jrpg-sprite-integration/11-CONTEXT.md
@.planning/phases/11-jrpg-sprite-integration/11-01-SUMMARY.md

<interfaces>
<!-- Current sprite generation and atlas structure -->
From scripts/generateSprites.ts:
```typescript
// Current: 16x16 frames, 10 cols x 4 rows per character sheet
// Layout: idle(1) + walk(4) + work(3) + talk(2) = 10 cols, 4 direction rows
const TILE = 16;
```

From src/engine/spriteAtlas.ts:
```typescript
// Current: frames at TILE_SIZE (16x16)
function makeFrame(col: number, row: number): SpriteFrame {
  return { x: col * T, y: row * T, w: T, h: T };
}
// CHARACTER_FRAMES maps state+direction to SpriteFrame[]
// ENVIRONMENT_ATLAS maps tile keys to SpriteFrame
// DECORATION_ATLAS maps decoration keys to SpriteFrame
```

From src/engine/types.ts:
```typescript
export const TILE_SIZE = 16;
export interface SpriteFrame { x: number; y: number; w: number; h: number; }
```

From src/engine/renderer.ts (how sprites are consumed):
```typescript
ctx.drawImage(sheet, frame.x, frame.y, frame.w, frame.h, x, y, TILE_SIZE, TILE_SIZE);
// ^ This line will be updated in Plan 03 to use CHAR_SPRITE_W/H for dest size
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite generateSprites.ts for 24x32 characters + 3/4 environment tiles</name>
  <files>
    scripts/generateSprites.ts,
    public/sprites/billy.png,
    public/sprites/patrik.png,
    public/sprites/marcos.png,
    public/sprites/sandra.png,
    public/sprites/isaac.png,
    public/sprites/wendy.png,
    public/sprites/environment.png
  </files>
  <action>
    Rewrite scripts/generateSprites.ts from scratch. The existing 673-line file generates 16x16 flat sprites. Replace with 24x32 JRPG 3/4 perspective character sprites and updated environment tiles.

    **Character sprite sheets (24x32 per frame):**
    - Canvas size per sheet: 10 columns x 4 rows = 240px x 128px
    - Same column layout: idle(1) + walk(4) + work(3) + talk(2) = 10 columns
    - Same row layout: down(0), left(1), right(2), up(3)
    - Each frame is 24px wide x 32px tall

    **Character rendering at 24x32 (JRPG 3/4 style):**
    - Head: ~8px tall at top (rows 0-7). Round-ish, with visible 2-pixel eyes, pixel mouth, hair detail
    - Torso: ~10px tall (rows 8-17). Outfit with collar/buttons/texture detail
    - Legs/feet: ~14px tall (rows 18-31). Pants/skirt + shoes at bottom
    - Characters face south (toward viewer) by default in idle frame
    - Walk frames: leg positions alternate (left-right-left-right pattern)
    - Work frames: slight lean forward, typing gesture at imaginary desk
    - Talk frames: mouth position varies, slight head tilt

    **Character appearance design (Claude designs each):**
    Use the CharacterColors interface. Design visually distinct characters:
    - BILLY: Dark hair, warm brown skin (#c8956a), gold/amber blazer over casual shirt. CEO creative exec look inspired by Billy Rovzar. Hair slightly tousled.
    - PATRIK (CFO): Neat combed hair, light brown skin, navy blue or dark suit. Conservative, precise appearance.
    - MARCOS (Lawyer): Distinguished, olive skin, dark hair with slight gray, charcoal suit. Professional.
    - SANDRA (Line Producer): Practical ponytail or bun, medium brown skin, casual-smart outfit (rolled sleeves). Efficient look.
    - ISAAC (Head of Development): Creative/relaxed, lighter skin, curly or messy hair, casual blazer or cardigan. Reader/creative type.
    - WENDY (Performance Coach): Warm, approachable, dark curly hair, earth-tone outfit. Professional but not corporate.

    **3/4 perspective rendering technique for 24x32:**
    - Body drawn as if viewed from slightly above and in front
    - Head slightly larger proportionally than realistic (chibi JRPG convention)
    - 1px outline on body for definition (dark version of dominant color)
    - Shoes are flat 2-3px at bottom of frame
    - Direction changes: front-facing (south) shows face; back-facing (north) shows back of head; left/right show profile

    **Environment tile sheet:**
    - Keep 16x16 for floor tiles (matching TILE_SIZE)
    - Furniture sprites: draw at natural 3/4 dimensions within 16x16 or multi-tile frames
    - Update canvas layout: 16 columns x 12 rows (expand from current 16x8 to accommodate new tiles)

    **Floor tiles (16x16, 3/4 perspective):**
    - 'floor-office': Warm parquet wood grain. Draw alternating light/dark oak pixel strips at slight angle to suggest wood planks. Colors: #c4a66a base, #b8975c dark grain, #d4b87a highlight.
    - 'floor-hallway': Slightly cooler/lighter tile floor. Subtle grid pattern. Colors: #b8b0a0 base, #a8a090 grid.
    - 'floor-warroom': Darker, richer wood. Conference room gravitas. Colors: #8b6e4e base, #7a5e3e dark.
    - 'door': Doorway floor tile (same as hallway but with threshold hint).

    **Wall tiles (16x16):**
    - 'wall-top': Cream wall with subtle texture. Keep existing color scheme (#d4c8a8).
    - 'wall-side': Side wall variant.
    - 'wall-window': Wall with small window detail.

    **Furniture sprites (16x16 per tile, 3/4 perspective with visible front faces):**
    - 'desk-left'/'desk-right': Wood desk top surface visible, front face shows drawer panel. Dark wood (#6b5035) with drawer handle detail.
    - 'chair': Viewed from above-and-front, shows seat and backrest curve.
    - 'bookshelf-top'/'bookshelf-bottom': Tall bookshelf (2 tiles), front face shows book spines in various colors.
    - 'table-segment': Conference table segment, dark premium wood (#4a3728) matching War Room gravitas.
    - 'monitor': Screen facing south (toward viewer), shows colored pixels suggesting content.
    - 'couch': New tile for Wendy's office — soft upholstered couch in earth tones, viewed from 3/4.
    - 'plant': Detailed potted plant with visible leaf fronds from above-and-front angle.
    - 'water-cooler': Blue jug on stand, 3/4 view.

    **Decoration sprites (16x16):**
    Update decoration keys to match new agents from Plan 01. Generate programmatic versions of:
    - patrik-chart: Financial chart/graph on wall
    - marcos-lawbooks: Stack of law books
    - sandra-schedule: Production schedule board
    - isaac-corkboard: Corkboard with pinned notes
    - wendy-motivational: Framed motivational piece
    - Plus existing generic decorations (artwork, filing-cabinet, post-it)

    **Run the script after writing:** `npx tsx scripts/generateSprites.ts`
    Verify all PNGs are generated in public/sprites/.
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx tsx scripts/generateSprites.ts && ls -la public/sprites/*.png | wc -l</automated>
    Should output 7 (billy, patrik, marcos, sandra, isaac, wendy, environment).
    Verify character PNGs are 240x128 pixels: `file public/sprites/billy.png`
  </verify>
  <done>
    - generateSprites.ts produces 24x32 character frames for all 6 characters
    - Each character has distinct appearance with visible face, hair, outfit
    - Environment sheet has warm parquet floors and 3/4 furniture with front-face depth
    - All 7 PNGs generated successfully in public/sprites/
    - Character sheets are 240x128 (10 cols x 4 rows of 24x32 frames)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update spriteAtlas.ts + types.ts for 24x32 character dimensions</name>
  <files>
    src/engine/spriteAtlas.ts,
    src/engine/types.ts
  </files>
  <action>
    **Step 1: Add character sprite constants to types.ts:**
    ```typescript
    /** Character sprite width (24px, wider than TILE_SIZE for JRPG 3/4 proportions) */
    export const CHAR_SPRITE_W = 24;
    /** Character sprite height (32px, taller than TILE_SIZE for JRPG 3/4 proportions) */
    export const CHAR_SPRITE_H = 32;
    ```

    **Step 2: Update spriteAtlas.ts character frame builder.**
    Import CHAR_SPRITE_W and CHAR_SPRITE_H from types.ts. Update makeFrame and buildCharacterFrames to use 24x32:
    - Character makeFrame: `{ x: col * CHAR_SPRITE_W, y: row * CHAR_SPRITE_H, w: CHAR_SPRITE_W, h: CHAR_SPRITE_H }`
    - Create separate makeCharFrame and makeEnvFrame functions (or parameterize) since environment tiles remain 16x16
    - Environment makeFrame stays at TILE_SIZE (16x16)

    **Step 3: Update CHARACTER_SHEET_NAMES** to reflect new agent names (should already be done in Plan 01, verify consistency): `['billy', 'patrik', 'marcos', 'sandra', 'isaac', 'wendy']`

    **Step 4: Update ENVIRONMENT_ATLAS** entries to match new sprite sheet layout from Task 1. Add any new tile types:
    - Add 'couch' entry for Wendy's office
    - Verify all furniture type keys match what officeLayout.ts FURNITURE references

    **Step 5: Update DECORATION_ATLAS** keys to match new agent decoration keys from Plan 01:
    - Replace diana-chart -> patrik-chart, diana-monitor -> patrik-monitor
    - Replace sasha-whiteboard-* -> sandra-schedule (simpler, single tile)
    - Add isaac-corkboard, wendy-motivational
    - Update coordinates to match the generated sprite sheet layout from Task 1

    **Step 6: Ensure SPRT-04 compliance** — the asset pipeline supports easy replacement. The contract is:
    - Drop a new PNG at public/sprites/{characterId}.png with same dimensions (240x128 for characters)
    - Same frame layout (10 cols x 4 rows)
    - No code changes needed — spriteAtlas.ts coordinates work for any art in the same layout
    - Document this contract in a code comment at the top of spriteAtlas.ts
  </action>
  <verify>
    <automated>cd /Users/quantumcode/CODE/BOILER-ROOM && npx tsc --noEmit 2>&1 | head -20</automated>
    TypeScript compiles. CHAR_SPRITE_W and CHAR_SPRITE_H are exported from types.ts. spriteAtlas.ts character frames use 24x32 dimensions.
  </verify>
  <done>
    - types.ts exports CHAR_SPRITE_W=24 and CHAR_SPRITE_H=32
    - spriteAtlas.ts CHARACTER_FRAMES use 24x32 frame dimensions
    - ENVIRONMENT_ATLAS includes 'couch' and all furniture types referenced by officeLayout.ts
    - DECORATION_ATLAS keys match new agent decoration keys
    - Asset swap documentation in code comment
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/generateSprites.ts` runs without error, generates all PNGs
2. Character PNGs are 240x128 pixels (10 * 24 wide, 4 * 32 tall)
3. `npx tsc --noEmit` passes
4. CHAR_SPRITE_W and CHAR_SPRITE_H constants are accessible throughout engine
5. Frame coordinates in spriteAtlas.ts match generated sprite sheet layout
</verification>

<success_criteria>
- 6 character sprite sheets at 24x32 per frame with visible face/outfit detail
- Environment tiles with warm parquet and 3/4 furniture front faces
- spriteAtlas.ts frame coordinates match generated PNGs exactly
- types.ts exports character sprite dimension constants
- Asset pipeline supports PNG swap without code changes (SPRT-04)
</success_criteria>

<output>
After completion, create `.planning/phases/11-jrpg-sprite-integration/11-02-SUMMARY.md`
</output>
