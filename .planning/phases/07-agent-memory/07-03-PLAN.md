---
phase: 07-agent-memory
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/memory/MemoryPanel.tsx
  - src/components/memory/__tests__/MemoryPanel.test.tsx
  - src/components/chat/ChatPanel.tsx
  - src/components/Header.tsx
autonomous: false

requirements: [MEM-03]

must_haves:
  truths:
    - "User can click a Memory button in the agent chat header to open the memory panel"
    - "Memory panel shows structured facts grouped by category with confidence badges"
    - "User can delete individual facts from the memory panel"
    - "Memory panel closes and returns to the chat view"
    - "Header shows memory fact count for current deal"
  artifacts:
    - path: "src/components/memory/MemoryPanel.tsx"
      provides: "Slide-over panel displaying agent memory facts"
      min_lines: 60
    - path: "src/components/memory/__tests__/MemoryPanel.test.tsx"
      provides: "Component tests for MemoryPanel"
  key_links:
    - from: "src/components/memory/MemoryPanel.tsx"
      to: "src/store/memoryStore.ts"
      via: "useMemoryStore to read and delete facts"
      pattern: "useMemoryStore"
    - from: "src/components/chat/ChatPanel.tsx"
      to: "src/components/memory/MemoryPanel.tsx"
      via: "conditional render with showMemory state"
      pattern: "MemoryPanel"
---

<objective>
Build the MemoryPanel UI and wire it into the ChatPanel for viewing and managing agent memory.

Purpose: Gives users visibility into what each agent "knows" about the current deal (MEM-03), enabling trust and correction of extracted facts.
Output: MemoryPanel component, ChatPanel memory button, Header memory count, human verification
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agent-memory/07-RESEARCH.md
@.planning/phases/07-agent-memory/07-01-SUMMARY.md

@src/components/chat/ChatPanel.tsx
@src/components/FileViewer.tsx
@src/components/Header.tsx
@src/store/memoryStore.ts
@src/types/memory.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/memory.ts:
```typescript
export type MemoryCategory = 'decision' | 'financial' | 'date' | 'action-item' | 'entity' | 'assumption' | 'risk' | 'term';

export interface MemoryFact {
  id: string;
  agentId: AgentId;
  dealId: string;
  category: MemoryCategory;
  content: string;
  confidence: 'high' | 'medium' | 'low';
  sourceAgentId: AgentId;
  createdAt: number;
  updatedAt: number;
}
```

From src/store/memoryStore.ts:
```typescript
export const useMemoryStore: UseBoundStore<StoreApi<MemoryState>>;
// Key selectors: facts, isExtracting, removeFact, getFactsForAgent
```

From src/components/FileViewer.tsx (pattern to follow):
```typescript
// Absolute inset-0 z-50 overlay pattern
<div className="absolute inset-0 z-50 flex flex-col bg-neutral-900 text-neutral-100 animate-[slideInRight_200ms_ease-out]">
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryPanel component + ChatPanel wiring</name>
  <files>src/components/memory/MemoryPanel.tsx, src/components/memory/__tests__/MemoryPanel.test.tsx, src/components/chat/ChatPanel.tsx, src/components/Header.tsx</files>
  <behavior>
    - Test 1: MemoryPanel renders facts grouped by category with category headers
    - Test 2: Each fact shows content, confidence badge (high=green, medium=amber, low=red), and relative time
    - Test 3: Delete button on each fact calls memoryStore.removeFact
    - Test 4: Close button calls onClose callback
    - Test 5: Empty state shows "No memories yet" message
  </behavior>
  <action>
    1. Create `src/components/memory/__tests__/MemoryPanel.test.tsx`:
       - Mock `useMemoryStore` to return controlled facts
       - Test all 5 behaviors above

    2. Create `src/components/memory/MemoryPanel.tsx`:
       - Props: `agentId: AgentId`, `onClose: () => void`
       - Read facts from `useMemoryStore` filtered by `agentId` and `activeDealId` (from `useDealStore`)
       - Group facts by `category` using a Map
       - Layout follows FileViewer pattern exactly: `absolute inset-0 z-50 flex flex-col bg-neutral-900`
       - Header: "Memory - {agentName}" with close button
       - Body: Scrollable list of category sections. Each section has a category header (capitalize, emoji prefix: decision=check, financial=dollar, date=calendar, action-item=arrow, entity=building, assumption=question, risk=warning, term=document). Each fact row shows: content text, confidence badge (high=green-600, medium=amber-600, low=red-600 as small pill), relative time (use same inline helper pattern as OverviewPanel), and a delete (trash) button.
       - Footer: "Close" button (same style as FileViewer close button)
       - Empty state: centered "No memories yet. Chat with {agentName} to build knowledge." message
       - Include `isExtracting` indicator: if `useMemoryStore.isExtracting`, show subtle "Extracting..." pulse in header

    3. Modify `src/components/chat/ChatPanel.tsx`:
       - Add import for MemoryPanel
       - Add `useState<boolean>` for `showMemory` in `AgentChatPanel`
       - Add a "Memory" button in the agent identity header (next to the processing indicator area). Use a brain/lightbulb icon or just text "Memory" with a small badge showing fact count for this agent.
       - When `showMemory` is true, render `<MemoryPanel agentId={agentId} onClose={() => setShowMemory(false)} />` as an absolute overlay (same z-50 pattern as FileViewer -- it overlays within the chat column container)
       - Memory button should show fact count: read from `useMemoryStore` filtered by current agentId + activeDealId

    4. Modify `src/components/Header.tsx`:
       - Add a small memory indicator showing total fact count for current deal (all agents)
       - Read from `useMemoryStore.getState().getFactsForDeal(activeDealId)` or use the selector pattern
       - Display as "{N} facts" next to the existing file count indicator
       - Only show when fact count > 0
  </action>
  <verify>
    <automated>npx vitest run src/components/memory/__tests__/MemoryPanel.test.tsx -x</automated>
  </verify>
  <done>MemoryPanel renders grouped facts with delete capability, ChatPanel has Memory button that opens overlay, Header shows fact count</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete memory system</name>
  <action>
    Human verifies the full end-to-end memory system: extraction, panel UI, context injection, and cross-agent attribution.
  </action>
  <verify>Human approval</verify>
  <done>User confirms memory extraction, panel display, deletion, and cross-agent attribution all work correctly</done>
  <what-built>
    Complete agent memory system: automatic fact extraction from conversations, memory panel UI, context injection, and cross-agent knowledge sharing.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Navigate to any agent room (e.g., Diana)
    3. Have a conversation mentioning specific facts: "The budget for Oro Verde is $2.4M USD with principal photography starting June 15"
    4. After the response completes, click the "Memory" button in the chat header
    5. Verify: extracted facts appear in the memory panel grouped by category (financial, date, etc.)
    6. Verify: each fact has a confidence badge and delete button
    7. Delete one fact and verify it disappears
    8. Close the memory panel
    9. Navigate to a different agent (e.g., Marcos)
    10. Have a conversation and check: the agent references Diana's facts with attribution ("Per Diana's analysis...")
    11. Check the Header: fact count should show total facts across all agents
    12. Verify in War Room: send a broadcast, check that all 5 agents extract facts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- MemoryPanel shows structured facts grouped by category
- Facts display confidence badges and relative timestamps
- Delete button removes individual facts
- Memory button in ChatPanel header shows fact count and opens panel
- Header shows total memory fact count for current deal
- Human verifies end-to-end: conversation -> extraction -> display -> cross-agent attribution
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-memory/07-03-SUMMARY.md`
</output>
