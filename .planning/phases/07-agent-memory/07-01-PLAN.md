---
phase: 07-agent-memory
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types/memory.ts
  - src/services/memory/extractMemory.ts
  - src/services/memory/__tests__/extractMemory.test.ts
  - src/store/memoryStore.ts
  - src/store/__tests__/memoryStore.test.ts
autonomous: true
requirements: [MEM-01, MEM-02]

must_haves:
  truths:
    - "Extraction service parses a user+assistant exchange and returns structured MemoryFact objects"
    - "memoryStore can add, load, remove, and query facts scoped by agentId and dealId"
    - "Extraction gracefully handles invalid JSON and empty responses"
    - "Deduplication prevents the same fact from accumulating multiple times"
  artifacts:
    - path: "src/types/memory.ts"
      provides: "MemoryFact, MemoryCategory types"
      exports: ["MemoryFact", "MemoryCategory"]
    - path: "src/services/memory/extractMemory.ts"
      provides: "LLM-based fact extraction service"
      exports: ["extractAndStoreMemory"]
    - path: "src/store/memoryStore.ts"
      provides: "Zustand store for memory facts"
      exports: ["useMemoryStore"]
  key_links:
    - from: "src/services/memory/extractMemory.ts"
      to: "src/services/anthropic/client.ts"
      via: "getAnthropicClient()"
      pattern: "getAnthropicClient"
    - from: "src/services/memory/extractMemory.ts"
      to: "src/store/memoryStore.ts"
      via: "useMemoryStore.getState().addFacts()"
      pattern: "useMemoryStore\\.getState\\(\\)"
    - from: "src/store/memoryStore.ts"
      to: "src/services/persistence/adapter.ts"
      via: "getPersistence() for IndexedDB memory store"
      pattern: "getPersistence"
---

<objective>
Create the memory type system, fact extraction service, and memory store with full TDD coverage.

Purpose: Foundation for all memory features -- types define the contract, extraction service converts conversations to structured facts, store persists and queries them.
Output: MemoryFact types, extractAndStoreMemory service, expanded memoryStore with CRUD + persistence
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agent-memory/07-RESEARCH.md

@src/types/agent.ts
@src/services/anthropic/client.ts
@src/services/context/summarizer.ts
@src/services/persistence/adapter.ts
@src/store/fileStore.ts
@src/types/persistence.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/agent.ts:
```typescript
export type AgentId = 'diana' | 'marcos' | 'sasha' | 'roberto' | 'valentina';
```

From src/types/persistence.ts:
```typescript
export type StoreName = 'conversations' | 'messages' | 'deals' | 'files' | 'memory';
```

From src/services/anthropic/client.ts:
```typescript
export function getAnthropicClient(): Anthropic;
```

From src/services/persistence/adapter.ts:
```typescript
export function getPersistence(): PersistenceAdapter;
```

From src/services/context/tokenCounter.ts:
```typescript
export const DEFAULT_MODEL = 'claude-sonnet-4-20250514';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryFact types + extraction service (TDD)</name>
  <files>src/types/memory.ts, src/services/memory/extractMemory.ts, src/services/memory/__tests__/extractMemory.test.ts</files>
  <behavior>
    - Test 1: extractAndStoreMemory calls Anthropic API with extraction prompt containing user+assistant text
    - Test 2: Valid JSON response is parsed into MemoryFact[] and stored via memoryStore.addFacts
    - Test 3: Empty array response (no facts) results in no store calls
    - Test 4: Invalid JSON response (markdown fences, explanatory text) is handled gracefully without throwing
    - Test 5: API error is caught and logged (non-fatal, never throws)
    - Test 6: Deduplication: if a fact with same category and overlapping content exists, update timestamp instead of adding duplicate
  </behavior>
  <action>
    1. Create `src/types/memory.ts` with MemoryCategory union type (decision, financial, date, action-item, entity, assumption, risk, term) and MemoryFact interface (id, agentId, dealId, category, content, confidence, sourceAgentId, createdAt, updatedAt). Use exact types from RESEARCH.md code examples.

    2. Create test file `src/services/memory/__tests__/extractMemory.test.ts`. Mock `getAnthropicClient` to return controlled responses. Mock `useMemoryStore.getState()` to track addFacts calls. Test all 6 behaviors above.

    3. Create `src/services/memory/extractMemory.ts` with:
       - `MEMORY_EXTRACTION_PROMPT` constant (use exact prompt from RESEARCH.md)
       - `extractAndStoreMemory(agentId, userMessage, assistantMessage, dealId)` async function
       - Uses `getAnthropicClient().messages.create()` with `DEFAULT_MODEL`, `max_tokens: 1024`
       - Parses response text via JSON.parse with fallback that strips markdown code fences
       - Maps extracted facts to MemoryFact objects with crypto.randomUUID(), timestamps
       - Calls deduplication check before storing: for each new fact, check existing facts (same agent+deal) for matching category + content substring overlap (>50% of words match). If match found, update existing fact's updatedAt. If no match, add as new.
       - Entire function wrapped in try/catch -- extraction failure is ALWAYS non-fatal (console.warn only)
  </action>
  <verify>
    <automated>npx vitest run src/services/memory/__tests__/extractMemory.test.ts -x</automated>
  </verify>
  <done>MemoryFact type exported, extractAndStoreMemory passes all 6 test cases, invalid JSON and API errors handled gracefully</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: memoryStore CRUD with persistence (TDD)</name>
  <files>src/store/memoryStore.ts, src/store/__tests__/memoryStore.test.ts</files>
  <behavior>
    - Test 1: loadFacts(dealId) queries IndexedDB 'memory' store by dealId index and populates state.facts
    - Test 2: addFacts(facts[]) persists each fact to IndexedDB and appends to state.facts
    - Test 3: removeFact(factId) deletes from IndexedDB and removes from state.facts
    - Test 4: getFactsForAgent(agentId, dealId) filters state.facts by both agentId and dealId
    - Test 5: getFactsForDeal(dealId) filters state.facts by dealId only (all agents)
    - Test 6: setExtracting(true/false) toggles isExtracting state
    - Test 7: loadFacts replaces existing facts (not appends) to prevent stale data on deal switch
  </behavior>
  <action>
    1. Create test file `src/store/__tests__/memoryStore.test.ts`. Mock `getPersistence()` to return an in-memory adapter. Test all 7 behaviors.

    2. Replace the stub in `src/store/memoryStore.ts` with full implementation. Follow the fileStore pattern exactly:
       - State: `facts: MemoryFact[]`, `isExtracting: boolean`
       - Actions: `loadFacts(dealId)`, `addFacts(facts[])`, `removeFact(factId)`, `getFactsForAgent(agentId, dealId)`, `getFactsForDeal(dealId)`, `setExtracting(boolean)`
       - `loadFacts` uses `getPersistence().query('memory', 'dealId', dealId)` and replaces (not appends) state.facts
       - `addFacts` iterates facts, calls `getPersistence().set('memory', fact.id, fact)` for each, then appends to state
       - `removeFact` calls `getPersistence().delete('memory', factId)` then filters state
       - `getFactsForAgent` and `getFactsForDeal` are synchronous getters filtering state.facts
  </action>
  <verify>
    <automated>npx vitest run src/store/__tests__/memoryStore.test.ts -x</automated>
  </verify>
  <done>memoryStore fully implemented with persistence, all 7 test cases pass, replaces previous stub</done>
</task>

</tasks>

<verification>
npx vitest run src/services/memory/__tests__/extractMemory.test.ts src/store/__tests__/memoryStore.test.ts --reporter=verbose
</verification>

<success_criteria>
- MemoryFact and MemoryCategory types exported from src/types/memory.ts
- extractAndStoreMemory is fire-and-forget safe (never throws, logs warnings)
- memoryStore has full CRUD with IndexedDB persistence via PersistenceAdapter
- All tests pass with mocked Anthropic client and persistence
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-memory/07-01-SUMMARY.md`
</output>
