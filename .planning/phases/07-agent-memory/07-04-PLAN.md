---
phase: 07-agent-memory
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/context/builder.ts
  - src/services/context/__tests__/builder.deal.test.ts
autonomous: true
gap_closure: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
  - MEM-05
  - MEM-06

must_haves:
  truths:
    - "buildContext with no active deal does NOT emit any text containing 'deal' in the system prompt"
    - "buildContext with an active deal still includes the Deal Creation Capability block"
    - "All 26 Phase 07 memory tests continue to pass"
    - "All 6 builder.deal.test.ts tests pass (regression fixed)"
  artifacts:
    - path: "src/services/context/builder.ts"
      provides: "Layer 5.5 Deal Creation Capability gated behind activeDealId"
      contains: "if (activeDealId)"
  key_links:
    - from: "src/services/context/builder.ts"
      to: "src/services/context/__tests__/builder.deal.test.ts"
      via: "Layer 5.5 conditional gate"
      pattern: "if.*activeDealId.*Deal Creation"
---

<objective>
Fix the test regression introduced in Phase 07 where the "Deal Creation Capability" block (Layer 5.5) in builder.ts is unconditionally injected into the system prompt, breaking the pre-existing builder.deal.test.ts assertion that the system prompt does NOT contain the word "deal" when no active deal is set.

Purpose: Restore the builder contract — when no deal is active, the system prompt must not contain deal-related context. This is a one-line gate fix.
Output: All builder tests green, all memory tests unaffected.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From src/services/context/builder.ts — the regression site -->
Lines 153-157: Layer 5.5 Deal Creation Capability block is pushed to layers[] unconditionally.
The fix: wrap lines 154-157 in `if (activeDealId) { ... }` since `activeDealId` is already available from line 50.

From src/services/context/__tests__/builder.deal.test.ts:
Line 69: `expect(result.systemPrompt).not.toContain('deal');` — this is the failing assertion when mockActiveDealId is null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Layer 5.5 behind activeDealId and verify all tests pass</name>
  <files>src/services/context/builder.ts, src/services/context/__tests__/builder.deal.test.ts</files>
  <action>
In `src/services/context/builder.ts`, wrap the Layer 5.5 "Deal Creation Capability" block (lines 153-157) inside an `if (activeDealId)` conditional, so it is only injected when a deal is active. The `activeDealId` variable is already destructured from `useDealStore.getState()` on line 50.

Change:
```typescript
// Layer 5.5: Deal creation capability
layers.push(`## Deal Creation Capability ...`);
```

To:
```typescript
// Layer 5.5: Deal creation capability (only when deal context is active)
if (activeDealId) {
  layers.push(`## Deal Creation Capability ...`);
}
```

Do NOT modify builder.deal.test.ts — the test assertion is correct and should pass after the builder fix.

After the fix, run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>npx vitest run src/services/context/__tests__/builder.deal.test.ts src/services/context/__tests__/builder.memory.test.ts src/components/memory/__tests__/MemoryPanel.test.tsx src/services/memory/__tests__/ src/store/__tests__/memoryStore.test.ts --reporter=verbose</automated>
  </verify>
  <done>builder.deal.test.ts passes 6/6 (including "with no active deal -> system prompt does NOT contain deal context"). All Phase 07 memory tests continue to pass. No regressions introduced.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — full test suite green
2. builder.deal.test.ts line 69 assertion passes (system prompt with no active deal does not contain 'deal')
3. builder.memory.test.ts — all 7 memory injection tests pass
4. No other test files affected
</verification>

<success_criteria>
- All builder.deal.test.ts tests pass (6/6), specifically the "no active deal" test
- All Phase 07 memory tests pass (26 total)
- Full test suite passes with zero failures
- Layer 5.5 Deal Creation Capability block only appears in system prompt when activeDealId is non-null
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-memory/07-04-SUMMARY.md`
</output>
