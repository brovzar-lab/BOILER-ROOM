---
phase: 13-polish-and-ui
plan: 03
type: execute
wave: 1
depends_on: ["13-00"]
files_modified:
  - src/components/chat/ChatPanel.tsx
  - src/components/chat/ChatInput.tsx
  - src/components/chat/TokenCounter.tsx
  - src/components/deal/DealSidebar.tsx
  - src/components/deal/DealCard.tsx
  - src/App.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Chat panel header shows agent name in signature color with thin color accent bar"
    - "Attach and Memory buttons are in the input area (not the header)"
    - "File count shows as small pill badge on Attach button"
    - "Token counter hidden by default, appears as progress bar when context > 60%"
    - "Thinking state shows typing indicator + subtle pulse on agent color bar"
    - "Deals sidebar is always visible on the left (not hidden behind toggle)"
    - "Sidebar collapses to thin 40px strip showing active deal name"
    - "Active deal name displayed as large bold text at top of expanded sidebar"
    - "Per-agent activity shows message count + 'Last active: Xh ago' per deal"
  artifacts:
    - path: "src/components/chat/ChatPanel.tsx"
      provides: "Redesigned agent header with accent bar, moved buttons to input area"
    - path: "src/components/chat/TokenCounter.tsx"
      provides: "Progress bar token counter, hidden until >60%"
    - path: "src/components/deal/DealSidebar.tsx"
      provides: "Always-visible sidebar with collapsed strip mode"
    - path: "src/components/deal/DealCard.tsx"
      provides: "Per-agent activity with message count and last active time"
    - path: "src/App.tsx"
      provides: "Updated layout: sidebar always rendered, no isOpen/onClose props"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/deal/DealSidebar.tsx"
      via: "DealSidebar always rendered without isOpen prop"
      pattern: "<DealSidebar"
    - from: "src/components/chat/ChatPanel.tsx"
      to: "src/components/chat/ChatInput.tsx"
      via: "Attach/Memory buttons passed down or rendered in input area"
      pattern: "ChatInput|Attach|Memory"
---

<objective>
Redesign the chat panel and deals sidebar to match modern chat app patterns -- inline input buttons, always-visible sidebar with collapsible strip, prominent deal display, and per-agent activity.

Purpose: Match production mockup quality with clean chat UX (Slack/ChatGPT patterns) and always-visible deal context.
Output: Redesigned ChatPanel.tsx, DealSidebar.tsx, DealCard.tsx, TokenCounter.tsx, updated App.tsx layout.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-polish-and-ui/13-CONTEXT.md
@.planning/phases/13-polish-and-ui/13-RESEARCH.md

<interfaces>
<!-- Current component interfaces -->

From src/components/deal/DealSidebar.tsx:
```typescript
interface DealSidebarProps {
  isOpen: boolean;    // TO BE REMOVED -- sidebar manages own collapsed state
  onClose: () => void; // TO BE REMOVED
}
// Current: w-60 when open, w-0 when closed
// getAgentActivity(dealId) already exists -- returns Record<string, number>
```

From src/components/deal/DealCard.tsx:
```typescript
interface DealCardProps {
  deal: Deal;
  isActive: boolean;
  agentActivity: Record<string, number>; // already passed, currently shown as dots
  onSelect: () => void;
}
// relativeTime(timestamp) helper already exists
```

From src/components/chat/ChatPanel.tsx:
```typescript
// Current header: color bar (w-1 h-8) + agent name/title + Attach button + Memory button
// Attach/Memory currently in header's ml-auto section
// AgentChatPanel rendered below header
// Hidden file input + drop zone overlay at bottom
```

From src/App.tsx:
```typescript
// Layout: Header > main flex row: DealSidebar(isOpen, onClose) | OfficeCanvas | ChatPanel
// sidebarOpen state controls DealSidebar visibility
// Header has onToggleSidebar callback
```

From src/components/chat/TokenCounter.tsx:
```typescript
// Currently shows token count as text -- needs replacement with progress bar
```

From src/store/chatStore.ts:
```typescript
// useChatStore has conversations: Record<string, Conversation>
// Each conversation has messages[], dealId, agentId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DealSidebar to always-visible with collapsible strip</name>
  <files>src/components/deal/DealSidebar.tsx, src/components/deal/DealCard.tsx, src/App.tsx</files>
  <action>
**DealSidebar.tsx:**

1. Remove `isOpen` and `onClose` props. Sidebar manages its own `collapsed` state via `useState(false)`.

2. When collapsed (`collapsed === true`), render a 40px-wide thin strip:
   - `w-10` full height, dark bg matching app theme, border-right.
   - Show a small expand chevron icon at top.
   - Show active deal name truncated vertically using `writing-mode: vertical-rl` (Tailwind: `[writing-mode:vertical-rl]`) or as a short horizontal truncation.
   - onClick on the strip: `setCollapsed(false)`.

3. When expanded (`collapsed === false`), render the full 240px sidebar:
   - **Active deal header section** at top: large bold text showing active deal name with a colored accent (left border or background tint in lemon-400). Use `text-lg font-bold text-[--color-text]` for the name.
   - Below header: deal list (existing sort by updatedAt desc).
   - Collapse button (chevron-left icon) in top-right corner that sets `setCollapsed(true)`.
   - Keep existing create deal form, archived toggle.

4. Transition between states: `transition-all duration-200` on the container width.

**DealCard.tsx:**

1. Update to show per-agent activity with message counts and last active time:
   - For each agent with activity > 0, show: agent color dot + agent name initial + message count.
   - Below the agent dots row, show "Last active: {relativeTime}" using the deal's `updatedAt`.
   - Keep the existing active-deal amber left border accent.
   - Format: compact row of colored circles with counts, e.g., `P:3 S:1 M:2` with each letter in the agent's signature color.

**App.tsx:**

1. Remove `sidebarOpen` state and `setSidebarOpen` toggle.
2. Render `<DealSidebar />` without props (no isOpen, no onClose).
3. Remove the sidebar toggle from Header props (or keep it but wire it to nothing -- check if Header uses it for anything else). If Header only uses `sidebarOpen` for the sidebar toggle button, remove that button from Header or repurpose it.
4. The sidebar is always in the DOM. The collapsed 40px strip replaces the w-0 hidden state.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>DealSidebar always visible (40px collapsed or 240px expanded); active deal name prominent at top; DealCard shows per-agent message counts; App.tsx no longer manages sidebar open/close state</done>
</task>

<task type="auto">
  <name>Task 2: Redesign chat panel header and move buttons to input area</name>
  <files>src/components/chat/ChatPanel.tsx, src/components/chat/ChatInput.tsx, src/components/chat/TokenCounter.tsx</files>
  <action>
**ChatPanel.tsx:**

1. Redesign agent identity header:
   - Thin color accent bar: change from `w-1 h-8` circle to a thin top border or left-edge bar spanning full width. Use `border-t-2` with `style={{ borderColor: agent.color }}` for a horizontal accent, OR keep vertical `w-1` but make it full height of the header. Use Claude's discretion per CONTEXT.md.
   - Agent name in signature color: `style={{ color: agent.color }}` on the name span.
   - Agent title below in muted text (keep existing pattern).
   - Remove Attach button and Memory button from the header entirely.
   - Remove the processing indicator from header (move to input area if needed).

2. Move Attach + Memory buttons to input area:
   - Pass `fileInputRef`, `onAttachClick`, `onMemoryClick`, `fileCount`, `factCount`, and `isProcessing` as props to ChatInput (or wrap ChatInput with these controls in ChatPanel).
   - Alternatively, render a button row just above or beside the ChatInput textarea:
     - Left side of input area: paperclip icon button (Attach) with file count pill badge, brain icon button (Memory).
     - The hidden file input stays in ChatPanel but the trigger button moves to input area.
   - File count pill badge: small circular badge `absolute -top-1 -right-1 bg-blue-500 text-white text-[9px] rounded-full w-4 h-4 flex items-center justify-center font-bold` on the Attach button. Only show when fileCount > 0.

3. Add thinking state indicator:
   - When agent status is 'thinking': show typing indicator dots (...) in the message area (can be a fake message at bottom of MessageList, or a status line above input).
   - Add subtle pulse animation on the agent color accent bar: `animate-pulse` class when status is 'thinking'.

4. War Room messages: ensure existing color-coding by agent is clean (no changes needed if already working -- just verify).

**ChatInput.tsx:**

If ChatInput is a separate component, update it to accept and render the Attach/Memory button props. Add a row of icon buttons to the left of (or below) the textarea.

**TokenCounter.tsx:**

1. Replace the text-based token counter with a progress bar:
   - Hidden by default (return null if percentage < 60).
   - When visible: thin horizontal bar (`h-1 w-full bg-neutral-800`) with colored fill.
   - Color: amber (`bg-amber-500`) for 60-80%, red (`bg-red-500`) for >80%.
   - Render as a thin strip above the input area or at the bottom of the header.
   - Use `transition-all` on the fill width for smooth updates.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Chat header shows agent name in signature color with accent bar; Attach/Memory buttons in input area with file count badge; token counter appears as progress bar only above 60%; thinking state shows typing indicator + color bar pulse; no TypeScript errors</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `npx vitest run` -- all tests pass
3. Visual: deals sidebar always visible, collapses to 40px strip with deal name
4. Visual: active deal name large and bold at sidebar top
5. Visual: DealCard shows per-agent message counts with colored initials
6. Visual: chat panel header has clean accent bar with agent name in signature color
7. Visual: Attach (with file badge) and Memory buttons in input area, not header
8. Visual: token counter hidden normally, shows as progress bar above 60%
</verification>

<success_criteria>
- Deals sidebar never fully hidden -- 40px collapsed strip or 240px expanded
- Active deal prominently displayed at sidebar top
- Chat panel header clean: accent color bar + agent name/title only
- Input area has Attach (paperclip + badge) and Memory (brain) icon buttons
- Token counter invisible until 60%, then appears as thin colored progress bar
- Thinking agents show typing indicator and pulse on color bar
- No regressions in existing chat functionality (messages, file drops, War Room)
</success_criteria>

<output>
After completion, create `.planning/phases/13-polish-and-ui/13-03-SUMMARY.md`
</output>
