---
phase: 06-file-handling
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/FileViewer.tsx
  - src/components/ChatPanel.tsx
  - src/components/Header.tsx
autonomous: false
requirements: [FILE-05]

must_haves:
  truths:
    - "User can click a file icon on the desk to open a slide-out viewer with extracted text"
    - "File viewer shows file name, size, upload date, and associated agent name"
    - "File viewer overlays the chat panel and closing it reveals chat underneath"
    - "User can delete a file from the viewer"
    - "Chat panel header shows file count indicator for the active agent"
    - "Files can be dropped onto the open chat panel as an alternative to canvas drop"
  artifacts:
    - path: "src/components/FileViewer.tsx"
      provides: "Slide-out panel with file metadata header and extracted text display"
      exports: ["FileViewer"]
    - path: "src/components/ChatPanel.tsx"
      provides: "Chat panel with drop zone and file count in header"
      exports: ["ChatPanel"]
  key_links:
    - from: "src/components/FileViewer.tsx"
      to: "src/store/fileStore.ts"
      via: "reads selected file, calls removeFile action"
      pattern: "useFileStore"
    - from: "src/components/ChatPanel.tsx"
      to: "src/store/fileStore.ts"
      via: "drop handler calls addFile, reads file count"
      pattern: "useFileStore"
---

<objective>
Build the file viewer slide-out panel, chat panel drop zone, and file count header indicator. Add human verification checkpoint for the complete file handling flow.

Purpose: Completes the user-facing file interaction -- viewing file contents, alternative drop target on chat panel, and visual file count indicator.
Output: FileViewer component, enhanced ChatPanel with drop zone, human-verified complete flow.
</objective>

<execution_context>
@/Users/quantumcode/.claude/get-shit-done/workflows/execute-plan.md
@/Users/quantumcode/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-file-handling/06-CONTEXT.md
@.planning/phases/06-file-handling/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/file.ts:
```typescript
export interface FileRecord {
  id: string;
  name: string;
  size: number;
  type: 'pdf' | 'docx';
  agentId: string;
  dealId: string;
  extractedText: string;
  uploadedAt: number;
}
```

From src/store/fileStore.ts:
```typescript
// State: files: FileRecord[], isProcessing: boolean
// Actions: addFile(file, agentId), removeFile(fileId), loadFiles(agentId, dealId), shareFile(fileRecord), getFilesByAgent(agentId)
```

From src/components/ChatPanel.tsx (existing pattern):
// Outer ChatPanel reads room store, routes to AgentChatPanel or WarRoomPanel or OverviewPanel
// Inner AgentChatPanel receives agent props, uses useChat hook

From src/components/Header.tsx (existing):
// Shows Lemon Command Center branding, active agent name/status/deal badge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FileViewer slide-out panel + ChatPanel drop zone + Header file count</name>
  <files>src/components/FileViewer.tsx, src/components/ChatPanel.tsx, src/components/Header.tsx</files>
  <action>
**FileViewer.tsx:**

Create a slide-out panel component (same position/style as ChatPanel -- right side, 400px wide) that overlays the chat panel with a higher z-index per CONTEXT.md decisions.

Props: `fileId: string | null, onClose: () => void`

When fileId is non-null:
- Read the FileRecord from fileStore by id
- Header section: file name (bold), file size (formatted as KB/MB), upload date (formatted with toLocaleDateString), agent name (look up from agent registry via agentId), file type badge (PDF red / DOCX blue)
- Body: extracted text rendered with whitespace preserved (pre-wrap). Use basic markdown-like formatting if possible, or plain text with good typography
- Footer: Close button (styled consistently with app theme) and Delete button (red accent)
- Delete button calls `useFileStore.getState().removeFile(fileId)` then calls `onClose()`
- "Share with all agents" button that calls `useFileStore.getState().shareFile(fileRecord)` -- show a brief confirmation after sharing

When fileId is null, render nothing.

Animation: slide in from right (translate-x transition, 200ms).

Styling: dark theme consistent with app (bg-neutral-900, text-neutral-100, amber accents for buttons).

**ChatPanel drop zone:**

Modify ChatPanel (the outer component or AgentChatPanel inner) to accept file drops:
- Add `onDragOver` handler that calls preventDefault and sets a visual state (e.g., a border highlight around the panel)
- Add `onDrop` handler that extracts files from dataTransfer, calls `useFileStore.getState().addFile(file, activeAgentId)` for each .pdf/.docx file
- Add `onDragLeave` handler that removes the visual highlight
- Visual feedback: thin amber border + "Drop file here" text overlay when dragging over the panel
- Only show drop zone when an agent room is active (not War Room, not BILLY's office, not overview)

**Header file count indicator:**

Modify Header component to show a file count badge when the active agent has files:
- Read files from fileStore filtered by active agentId and active dealId
- If count > 0, show a small icon (document emoji or a styled span) + count (e.g., "3 files") next to the agent name
- Clicking the indicator could open a file list dropdown (or simply be informational for now)

**Canvas file click -> FileViewer wiring:**

Add state management for the selected file. In the OfficeCanvas or App component:
- Track `selectedFileId: string | null` in local state
- When a file icon is clicked on the canvas (detected via hoverTileCol/hoverTileRow matching a file icon position), set selectedFileId
- For canvas click detection: in `input.ts` handleClick, check if the click is on a file icon position (compare click tile to desk area where icons are rendered). If a file icon is clicked, set a module-level `clickedFileId` variable that the React component can poll or use a callback.
- Alternative approach: add a `onFileClick: (fileId: string) => void` callback parameter to setupInputHandlers and call it from handleClick when a file icon position is detected.
- Render `<FileViewer fileId={selectedFileId} onClose={() => setSelectedFileId(null)} />` in App.tsx or ChatPanel, positioned above the chat panel (z-50 vs chat panel's z-40).

**Processing indicator:**

When fileStore.isProcessing is true, show a small pulsing indicator on the active agent's desk area or in the chat panel header -- a subtle "Processing file..." text or a pulsing document icon. Per CONTEXT.md: "Document icon on the desk pulses/animates until extraction completes. No toast notification."
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose -x</automated>
  </verify>
  <done>FileViewer opens when clicking file icons, shows metadata and extracted text, delete works. ChatPanel accepts file drops with visual feedback. Header shows file count. Processing indicator visible during extraction. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete file handling flow</name>
  <files>n/a</files>
  <action>
User manually verifies the complete file handling flow across all Phase 6 requirements.

What was built: Complete file handling flow -- drag-and-drop PDF/DOCX onto canvas desks or chat panel, text extraction via pdfjs-dist and mammoth, file icons on desks with hover tooltips, file viewer slide-out panel with metadata and delete, context injection into agent system prompts, file count header indicator.

How to verify:
1. Start dev server: `npm run dev`
2. Navigate to an agent room (e.g., click Diana's office)
3. Drag a PDF file onto the canvas over Diana's desk -- verify amber highlight appears during drag
4. Drop the PDF -- verify a file icon appears on Diana's desk after processing
5. Hover over the file icon -- verify filename tooltip appears
6. Click the file icon -- verify FileViewer slide-out opens with file name, size, date, extracted text
7. Close the viewer -- verify chat panel is visible underneath
8. Drag a DOCX file onto the open chat panel -- verify it is accepted with visual feedback
9. Send a message to Diana asking about the file content -- verify she references the uploaded document
10. Check the header -- verify file count indicator shows (e.g., "2 files")
11. Open the file viewer and click Delete -- verify the file is removed from desk and viewer closes
12. Try dragging a file onto the War Room or hallway -- verify it is silently ignored (no highlight)
13. Try dragging a .txt file -- verify it is ignored (unsupported type)
  </action>
  <verify>Human confirms all 13 verification steps pass</verify>
  <done>User types "approved" confirming complete file handling flow works end-to-end</done>
</task>

</tasks>

<verification>
Full manual verification of the complete file handling flow across all 6 FILE requirements.
```bash
npx vitest run --reporter=verbose
```
All automated tests pass. Manual verification confirms visual and interactive elements.
</verification>

<success_criteria>
- FileViewer shows file metadata + extracted text in slide-out panel
- Chat panel accepts file drops as alternative to canvas drops
- Header shows file count for active agent
- File icons clickable to open viewer
- Delete removes file from storage and UI
- Agent responses reference uploaded file content (context injection working)
- All 6 FILE requirements verified
</success_criteria>

<output>
After completion, create `.planning/phases/06-file-handling/06-03-SUMMARY.md`
</output>
